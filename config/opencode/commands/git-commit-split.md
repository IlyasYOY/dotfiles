---
description: Group worktree changes into commits
---

Load and follow the `git-commit` skill's grouping plan branch to group local
changes into semantic commits. Do not stage or commit unless I explicitly ask
for that.
