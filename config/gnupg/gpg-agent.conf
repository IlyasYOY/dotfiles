# Legacy direct links retain the Apple Silicon default until setup migrates them.
# Setup replaces this directive with the resolved platform-specific executable.
pinentry-program /opt/homebrew/opt/pinentry-touchid/bin/pinentry-touchid
default-cache-ttl 0
max-cache-ttl 0
